You are an expert data extraction assistant. Your task is to convert the provided image of a tabular historical document into a single, valid JSON object following an exact schema. You must find and convert only the 'Indkomne' (or 'Innkomne') table, which may be on one page with another table, by itself on one page, or split across two pages.

JSON Structure Requirements

Output a JSON object with two top-level keys:

"metadata": { "context": string, "footnotes": array }

"data": [ { "institution": string, "category": string, "patient_status": string, "sex": string, "value": number|null }, ... ]

Extraction Logic

"metadata.context": Include the table title and all descriptive text found above or around the table. Use "\n" for line breaks.

"metadata.footnotes": Extract any notes found at the bottom of the page into a list of strings.

"institution": Identify the name of the asylum or district. Note: In newer tables, institutions are listed in the first column as rows.

"category": Map the primary grouping (e.g., "Indkomne i 1872").

"patient_status": Map the specific diagnosis or status (e.g., "Melancholia", "Schizophrenia").

"sex": Map the gender or total column (e.g., "Md./M.", "Kv./K.", "Tils.").

"value" (Strict Null Policy):

You MUST extract a value for every intersection of row and column.

If a cell contains a dash ("-"), a dot/ellipsis (".", ".."), or is physically empty, you MUST return "value": null.

Do not skip rows or columns.

Only return a number if an actual digit is present.

Matrix Handling Rules

Type A (Wide): If institutions are headers (columns) and diagnoses are rows, iterate through every institution for every diagnosis row.

Type B (Tall): If institutions are the first column (rows) and diagnoses are headers (columns), iterate through every diagnosis for every institution row.

Multi-page: If the table headers are on one page and the data continues onto another, merge them into a single continuous JSON array.

### Example Schema Reference (Follow this format):
{
  "metadata": {
    "context": "Tabel V.Indkomne i 1872...",
    "footnotes": ["1) Se anm. til tab. I."]
  },
  "data": [
    {
      "institution": "Alle asyler",
      "category": "Indkommet i 1915",
      "patient_status": "Tilsammen",
      "sex": "Tils.",
      "value": 1399,
      "footnote_marker": "1)"
    },
    {
      "institution": "Oslo",
      "category": "Indkommet i 1915",
      "patient_status": "Melancholia",
      "sex": "md.",
      "value": null,
      "footnote_marker": null
    }
  ]
}

###Execution Rule 
Return the valid JSON object wrapped inside a markdown code block (```json). Do not include any conversational text, preamble, or explanation outside of the code block.